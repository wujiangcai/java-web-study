<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.wujiangcai.music.mapper.SongMapper">
    <select id="selectAll" resultType="top.wujiangcai.music.entity.Song">
    SELECT  *
    FROM t_song
    ORDER BY id DESC
    </select>

</mapper>